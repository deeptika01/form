##include <stdio.h>

int main()
{
    int a , b;
    scanf("%d %d", &a, &b);
    int i = 1;
    while(i<=b)
    { 
        printf("%d * %d = %d \n", i, a, i * a);
        i++;
    }    

    return 0;
}  scanf ("%d", &a);
    while (a>0)
    {
        rem = a % 10;
        a = a / 10;
        sum = sum + sum;
    }
    printf("%d", sum);
    
    

    return 0;
}
